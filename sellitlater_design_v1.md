# 売るもの管理アプリ 設計書 v1.0

## 1. プロジェクト概要

### 1.1 アプリ概要
「いつかメルカリで売る物」を管理する一般ユーザー向けアプリ。家に眠っている売れる物を資産として見える化し、「全部売ったらいくらになるか」をホーム画面で示すことを最重要価値とする。

### 1.2 ターゲット
- メルカリでたまに物を売る人
- 使っていない物をあとで売りたい人
- 保管場所を忘れがちな人

非対象：転売業者、業務在庫管理ユーザー、複数フリマ運用ユーザー。

### 1.3 技術スタック
- **フレームワーク**: React Native + Expo SDK（Bare Workflow / prebuild方式）
  - Expo SDKライブラリは使用するが、EAS Buildには依存しない
  - `npx expo prebuild` でネイティブプロジェクトを生成し、Gitで管理
- **言語**: TypeScript
- **ローカルDB**: expo-sqlite + Drizzle ORM（型安全のため）
  - 代替案: WatermelonDB（大量データに強いが、今回は20件〜数百件想定なのでSQLiteで十分）
- **状態管理**: Zustand（軽量、Redux不要）
- **画像保存**: expo-file-system（端末ローカルに保存、DBにはパスのみ保存）
- **ナビゲーション**: expo-router（タブ＋スタック構成）
- **課金**: react-native-iap
- **広告**: react-native-google-mobile-ads (AdMob)
- **ビルド/配信**: GitHub Actions (macOS runner) + fastlane
  - ビルド: xcodebuild
  - 証明書管理: fastlane match（Gitリポジトリで証明書を共有・管理）
  - TestFlight配信: fastlane pilot
  - EAS Build / EAS Submit には依存しない

### 1.4 対応プラットフォーム
- iOS のみ（App Store配信）
- 最低対応OS: iOS 15以上

---

## 2. データモデル

### 2.1 Item（商品）
| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| id | string (UUID) | ○ | 主キー |
| name | string | ○ | 商品名 |
| category | string | ○ | カテゴリ（後述の固定リスト＋カスタム） |
| purchasePrice | number \| null | - | 購入価格 |
| expectedPrice | number | ○ | 見込み売却価格 |
| shippingFee | number | - | 送料（デフォルト0） |
| location | string | - | 保管場所（自由入力） |
| condition | enum | ○ | `new` / `good` / `normal` / `damaged` |
| status | enum | ○ | `maybe` / `preparing` / `listed` / `sold` / `hold` |
| memo | string | - | メモ |
| isFavorite | boolean | ○ | お気に入り（★）。デフォルトfalse |
| createdAt | datetime | ○ | 登録日時 |
| updatedAt | datetime | ○ | 更新日時 |

### 2.2 ItemImage（商品画像）
| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| id | string (UUID) | ○ | 主キー |
| itemId | string (FK) | ○ | Itemへの外部キー |
| filePath | string | ○ | ローカルファイルパス |
| order | number | ○ | 表示順 |
| createdAt | datetime | ○ | 作成日時 |

※ 無料版は1商品あたり1枚まで、Pro版は複数枚

### 2.3 SaleRecord（売却記録）
売却済みになった商品の実績データ。
| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| id | string (UUID) | ○ | 主キー |
| itemId | string (FK, UNIQUE) | ○ | Itemへの外部キー |
| actualPrice | number | ○ | 実際の売却価格 |
| actualShippingFee | number | ○ | 実送料 |
| actualProfit | number | ○ | 実利益（計算済みを保存） |
| soldAt | date | ○ | 売却日 |
| createdAt | datetime | ○ | 記録日時 |

### 2.4 カテゴリ（固定リスト）
初期値として以下を提供。ユーザー追加も可能。
- ファッション
- 家電
- スマホ・タブレット
- 本・漫画
- ゲーム
- おもちゃ
- ホビー
- ベビー・キッズ
- インテリア
- スポーツ
- その他

### 2.5 Setting（設定）
| フィールド | 型 | 説明 |
|---|---|---|
| isPro | boolean | Pro版フラグ |
| theme | enum | `light` / `dark` / `system` |
| feeRate | number | メルカリ手数料率（デフォルト0.10） |

---

## 3. 計算ロジック

### 3.1 見込み利益
```
見込み利益 = expectedPrice - (expectedPrice × feeRate) - shippingFee
```

### 3.2 利益率
```
利益率 = 見込み利益 ÷ expectedPrice × 100（%）
```
※ 詳細画面・追加画面で見込み利益と並べて表示

### 3.3 実利益
```
実利益 = actualPrice - (actualPrice × feeRate) - actualShippingFee
```
※ 売却記録時に計算してSaleRecord.actualProfitに保存

### 3.4 ホーム画面の集計
- **想定売上合計**: status が `sold` 以外の全Itemの `expectedPrice` 合計
- **想定利益合計**: 上記アイテムの見込み利益合計
- **出品中件数**: status が `listed` のItem数
- **売却済み利益**: 全SaleRecordの `actualProfit` 合計
- **登録数**: 全Item数

---

## 4. 画面構成と遷移

### 4.1 タブ構成（5タブ）
1. **ホーム** (`/home`)
2. **一覧** (`/list`)
3. **追加** (`/add`)
4. **分析** (`/analytics`)
5. **設定** (`/settings`)

### 4.2 画面遷移概要

```
[ホーム]
  ├─ 最近追加した商品タップ → [商品詳細]
  └─ "全て見る"等 → [一覧]タブへ

[一覧]
  ├─ 商品タップ → [商品詳細]
  ├─ 並び替えボタン → モーダル
  ├─ 絞り込みボタン → モーダル
  └─ 表示切替（グリッド/リスト）

[追加]
  └─ 保存 → [ホーム]へ戻る or [商品詳細]へ

[商品詳細]
  ├─ 編集ボタン → [編集画面]
  ├─ "売却済みにする"ボタン → [売却記録入力モーダル] → ホーム/一覧へ
  └─ 削除ボタン → 確認ダイアログ → 戻る

[編集画面]
  └─ 保存 → [商品詳細]へ戻る

[分析]
  └─ Pro版限定。無料版はアップグレード誘導画面

[設定]
  ├─ Proにアップグレード → [課金画面]
  ├─ テーマ → 選択モーダル
  ├─ データ初期化 → 確認ダイアログ
  └─ アプリ情報 → 静的画面
```

### 4.3 各画面の構成要素

※ 各要素はデザインモック画像に準拠。以下は要素の列挙であり、レイアウト・色・余白はモックに従う。

#### ホーム画面
- ヘッダー: 「ホーム」タイトル（通知ベル・メニューはv1.0では非機能/非表示）
- 資産サマリーカード（「すべて売却した場合の想定」）
  - 想定売上合計（大きく表示・最重要）
  - 想定利益合計
  - 出品中 / 出品準備中 / 保管中 件数、売却済み利益
- ステータス別セクション（保管中/出品準備中/出品中/売却済み/保留 の件数とバー）
- カテゴリ別 想定売上（ドーナツグラフ＋内訳リスト）
- 最近追加した商品（横スクロールカードリスト、ステータスバッジ付き）

#### 一覧画面
- ヘッダー: 検索アイコン、絞り込みアイコン
- 表示切替トグル（グリッド/リスト）
- 並び替えセレクタ（登録日が新しい順/金額/売却予定額/ステータス）
- 絞り込みセレクタ（カテゴリ/保管場所/ステータス）
- 全件数表示、「長押しで複数選択」
- 商品カード（写真、ステータスバッジ、お気に入り★、商品名、見込み売却価格、見込み利益、保管場所、登録日）
- フローティング「商品を追加」ボタン

#### 追加画面
- ヘッダー: キャンセル / 「商品を追加」 / 保存
- 写真選択（1枚目のサムネ＋追加ボタン。無料1枚、Pro複数。「並び替え」はPro時）
- 商品名（必須、文字数カウンタ）
- カテゴリ（必須）
- 価格情報: 購入価格、見込み売却価格（必須）、送料（必須）
  - メルカリ手数料(10%)、見込み利益、利益率を自動計算プレビュー
- 商品の詳細: 状態（必須）、ステータス（必須）、保管場所、メモ
- 保存ボタン / 「続けてもう1つ追加」ボタン

#### 商品詳細画面
- ヘッダー: 戻る / 「商品詳細」 / 編集
- 写真カルーセル（複数枚はサムネ列、お気に入り★）
- 商品名、ステータスバッジ、保管場所、登録日
- 見込み売却価格 / 見込み利益 / 利益率
- 購入価格 / 送料（インライン編集可）/ メルカリ手数料(10%)
- カテゴリ / 状態 / ステータス / 保管場所 / メモ（各行タップで編集）
- 売却済みの場合の記録セクション（「売却済みにする」ボタン、実売却価格/実送料/実利益/売却日）
- フッター: 分析を見る / 削除（※「複製」「シェア」はv1.0では非実装）

#### 売却記録入力モーダル
- 実際の売却価格、実送料、売却日
- 実利益（自動計算プレビュー）
- 保存

#### 分析画面（Pro限定）
- ヘッダー: 「分析」、期間セレクタ（今月など）
- タブ: サマリー / 想定データ / 売却済みデータ
- サマリー指標（想定売上合計、想定利益合計、売却済み利益、売却済み件数）
- カテゴリ別 想定売上（ドーナツグラフ＋内訳）
- 月別 売却済み利益の推移（折れ線グラフ）
- カテゴリ別 売却済み利益（横棒）
- ステータス別 件数（ドーナツグラフ）
- 月別 売却実績サマリー（テーブル：月/売却件数/売却価格合計/送料合計/利益合計）

#### 設定画面
- Proプラン導線（「プランを確認」）
- アカウント・データ: データの削除（※バックアップ/エクスポート/アカウントはv1.0では非実装）
- 表示・カスタマイズ: テーマカラー、利益の計算方法、ステータスの編集、カテゴリの編集（※通貨設定はv1.0では非実装、円固定）
- サポート・その他: よくある質問、お問い合わせ、利用規約、プライバシーポリシー、アプリについて（バージョン）
- （※通知設定はv1.0では非実装）

---

## 4.4 モックにあるがv1.0では実装しない要素（claude designへの明示事項）

以下はデザインモックに描かれているが、v1.0方針により**実装しない**。UI構築時にこれらの要素は省略すること。
- 設定: データのバックアップ
- 設定: データのエクスポート
- 設定: アカウント
- 設定: 通貨の設定（円 JPY 固定）
- 設定: 通知設定
- ホーム: 通知ベルアイコン（機能なし。表示するだけのダミーも不要）
- 商品詳細: シェアボタン（SNS機能のため）
- 商品詳細: 複製ボタン

---

## 5. Pro版制限のハンドリング

無料版での制限は以下で実装：
- **登録数20件超過時**: 追加ボタン押下時にアップグレード誘導モーダル
- **写真複数枚**: 写真選択UIで2枚目以降にロックアイコン、タップでアップグレード誘導
- **分析画面**: タブを開くとアップグレード誘導画面を表示
- **広告**: ホーム画面下部・一覧画面下部にバナー広告

`Setting.isPro` を真偽値で持ち、各画面の表示/動作を分岐。

---

## 6. 開発順序（Claude Codeへの指示単位）

Claude Codeに段階的に依頼する4ステップ構成。各ステップ完了時に必ず動作確認してから次へ進むこと。

### 共通の前提
- **最初に**この設計書とデザインモック画像を全てClaude Codeに渡す
- 各ステップで「指示範囲を超えて先回り実装しない」よう明示する
- ステップ完了時にコミットを切る

---

### STEP 1: 全画面のUI構築（データなし）
**目的**: デザインモック通りの見た目を全画面で完成させる。

実装範囲:
1. Expo + TypeScript + expo-router プロジェクト初期化
2. 5タブ構成のスケルトン
3. 以下の全画面をハードコードのダミーデータで構築:
   - ホーム / 一覧 / 追加 / 分析 / 設定
   - 商品詳細 / 編集 / 売却記録入力モーダル
4. 画面遷移の配線

**完了条件**: 全画面がデザインモック通りに表示され、画面遷移が動く。

⚠️ **このステップでデザイン再現度を完全に確認すること**。後工程で修正が入ると手戻りが大きい。

---

### STEP 2: データ基盤 + コア体験の実装
**目的**: コア体験（追加→ホームに反映→詳細→売却記録）を実データで動作させる。

実装範囲:
1. SQLite + Drizzle ORM スキーマ定義・マイグレーション
2. Zustand ストア設計
3. 写真機能（expo-image-picker + ファイル保存、無料版仕様の1枚のみ）
4. ダミーデータから実データへの差し替え:
   - 商品の追加・編集・削除
   - 一覧（基本表示のみ。並び替え・絞り込みは次ステップ）
   - ホーム画面の集計表示
   - 商品詳細
   - 売却ステータス変更＋売却記録入力

**完了条件**: 商品を追加→ホームに金額が反映→詳細表示→売却済み記録、までが全て動く。

---

### STEP 3: 機能拡充
**目的**: 残りの機能を完成させる。

実装範囲:
1. 一覧画面の並び替え（登録日/金額/売却予定額/ステータス）
2. 一覧画面の絞り込み（カテゴリ/保管場所/ステータス）
3. 一覧画面のグリッド/リスト表示切替
4. 分析画面（カテゴリ別売上グラフ、月別売却実績グラフ、売却済み利益累計、売却数）
5. 設定画面の機能（テーマ切替、データ初期化、アプリ情報）

**完了条件**: Pro制限なしの完全版アプリとして動作する。

---

### STEP 4: 課金・広告・リリース準備
**目的**: 配信可能な状態にする。

実装範囲:
1. react-native-iap 統合（買い切り1,500円 + サブスク月額480円）
2. Pro版判定ロジック（Setting.isPro）
3. 各画面のPro版制限適用:
   - 21件目追加時のアップグレード誘導
   - 写真2枚目以降のロック
   - 分析画面のロック
4. AdMob バナー広告配置（無料版のみ、ホーム画面下部・一覧画面下部）
5. アプリアイコン、スプラッシュ画面
6. App Store メタデータ準備
7. `npx expo prebuild --platform ios` でネイティブプロジェクト生成、Git管理
8. fastlane セットアップ:
   - `fastlane match`（証明書・プロビジョニングプロファイル管理用のプライベートリポジトリ作成）
   - `fastlane pilot`（TestFlight配信）
   - Fastfile に build / beta レーンを定義
9. GitHub Actions ワークフロー作成:
   - `runs-on: macos-latest`
   - mainブランチへのpushまたはタグ作成でトリガー
   - GitHub Secretsで管理する値:
     - `MATCH_PASSWORD`（fastlane matchの暗号化キー）
     - `MATCH_GIT_BASIC_AUTHORIZATION`（matchリポジトリへのアクセス）
     - `APP_STORE_CONNECT_API_KEY_*`（App Store Connect API Key、Issuer ID、Key ID）
   - ステップ: チェックアウト → Node/Ruby/Xcodeセットアップ → 依存インストール → `fastlane match appstore --readonly` → `fastlane beta`

**完了条件**: GitHub Actionsでビルドが成功し、TestFlightに自動配布される。Pro版制限・課金・広告が正しく動作する。

---

## 7. ディレクトリ構成（推奨）

```
src/
  app/                    # expo-routerのルート
    (tabs)/
      home.tsx
      list.tsx
      add.tsx
      analytics.tsx
      settings.tsx
      _layout.tsx
    item/
      [id].tsx           # 商品詳細
      [id]/edit.tsx      # 商品編集
    _layout.tsx
  components/             # 共通UIコンポーネント
  features/               # 機能別モジュール
    item/
    sale/
    analytics/
    purchase/             # 課金
  db/
    schema.ts             # Drizzleスキーマ
    migrations/
    queries.ts
  stores/                 # Zustandストア
  utils/
    calculations.ts       # 利益計算など
  constants/
    categories.ts
```

---

## 8. v1.0で実装しないもの（再確認）
iCloud同期、CSVエクスポート、バックアップ、eBay連携、複数フリマ同期、バーコードDB、ラベル印刷、税務管理、マイレージ管理、Web版、SNS機能、転売業務向け機能。

---

## 9. 補足

### 9.1 画面デザイン
別途モックが存在するため、Claude Codeへの指示時にモック画像を併せて提供すること。

### 9.2 利益計算の注意
メルカリ手数料率はSetting.feeRateで保持。将来のメルカリ手数料変更や他フリマ対応の余地を残す。デフォルトは0.10（10%）。

### 9.3 データ永続化
- DBファイル: expo-sqliteがアプリのドキュメントディレクトリに自動配置
- 画像: `FileSystem.documentDirectory/images/` 配下にUUIDで保存
- データ初期化時: DBレコード削除＋画像ディレクトリ削除を両方実行
